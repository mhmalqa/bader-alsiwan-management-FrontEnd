'use client'
import Link from 'next/link'
import { useEffect, useState } from 'react'
import { DataTable } from '@/components/shared/data-table'
import { listManagementContracts, type ManagementContract } from './api'
export function ManagementContractsListPage() { const [items,setItems]=useState<ManagementContract[]>([]); const [loading,setLoading]=useState(true); const [error,setError]=useState(''); useEffect(()=>{void listManagementContracts().then(({items})=>setItems(items)).catch(()=>setError('تعذر تحميل عقود الإدارة.')).finally(()=>setLoading(false))},[]); return <div className="content"><div className="module-title"><div><span className="overline">علاقة الإدارة</span><h1>عقود الإدارة</h1><p>بيانات العقود من الخادم.</p></div><Link className="add-button" href="/management-contracts/new">إضافة عقد إدارة</Link></div>{loading?<p>جارٍ تحميل العقود…</p>:error?<p role="alert" className="error-message">{error}</p>:<DataTable rows={items} columns={[{key:'contractNumber',title:'رقم العقد'},{key:'ownerId',title:'المالك'},{key:'startDate',title:'البداية'},{key:'endDate',title:'النهاية'},{key:'feeMethod',title:'الرسوم'},{key:'status',title:'الحالة'},{key:'id',title:'الإجراء',render:(_,row)=><Link className="cancel-button" href={`/management-contracts/${row.id}`}>عرض</Link>}]}/>}</div> }
