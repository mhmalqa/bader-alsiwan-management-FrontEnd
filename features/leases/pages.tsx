import Link from 'next/link'
import { leases, properties, tenants, units } from '@/mocks/data'
const frequencyLabel = { annual: 'سنوي', semi_annual: 'نصف سنوي', quarterly: 'ربع سنوي', monthly: 'شهري', one_time: 'دفعة واحدة', custom: 'مخصص' }
export function LeaseDetailsPage({ id }: { id: string }) { const lease = leases.find((item) => item.id === id); if (!lease) return <div className="content"><h1>عقد الإيجار غير موجود</h1></div>; const tenant = tenants.find((item) => item.id === lease.tenantId); const property = properties.find((item) => item.id === lease.propertyId); const unit = units.find((item) => item.id === lease.unitId); return <div className="content"><div className="module-title"><div><span className="overline">عقد إيجار</span><h1>{lease.internalContractNumber}</h1><p>{tenant?.fullName} · {property?.propertyName} · {unit?.unitNameOrNumber}</p></div><Link className="add-button" href={`/leases/${id}/edit`}>تعديل العقد</Link></div><section className="card"><h2>بيانات العقد</h2><div className="financial-grid"><span>البداية <b>{lease.startDate}</b></span><span>النهاية <b>{lease.endDate}</b></span><span>قيمة العقد <b>{lease.totalContractValue.toLocaleString('ar-SA')} ر.س</b></span><span>دورية السداد <b>{frequencyLabel[lease.paymentFrequency]}</b></span></div></section><section className="card table-card"><h2>جدول الأقساط</h2><div className="table-wrap"><table><thead><tr><th>القسط</th><th>تاريخ الاستحقاق</th><th>المبلغ</th><th>ملاحظات</th></tr></thead><tbody>{lease.installments.map((item) => <tr key={item.installmentNumber}><td>{item.installmentNumber}</td><td>{item.dueDate}</td><td>{item.amount.toLocaleString('ar-SA')} ر.س</td><td>{item.notes ?? '—'}</td></tr>)}</tbody></table></div></section></div> }
