import Link from 'next/link'
export type FeatureRow = { id: string; title: string; subtitle: string; status: string; amount?: number }
export function FeaturePage({ title, description, rows, actionLabel, actionHref }: { title: string; description: string; rows: FeatureRow[]; actionLabel?: string; actionHref?: string }) { return <div className="content"><div className="module-title"><div><span className="overline">تشغيل وإدارة</span><h1>{title}</h1><p>{description}</p></div>{actionLabel && actionHref && <Link className="add-button" href={actionHref}>{actionLabel}</Link>}</div><section className="card table-card"><div className="table-wrap"><table><thead><tr><th>العنوان</th><th>التفاصيل</th><th>الحالة</th><th>القيمة</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>{row.title}</td><td>{row.subtitle}</td><td>{row.status}</td><td>{row.amount ? `${new Intl.NumberFormat('ar-SA').format(row.amount)} ر.س` : '—'}</td></tr>)}</tbody></table></div></section></div> }
