import { ManagementContractDetailsPage } from '@/features/management-contracts/detail-page'; export default async function Page({ params }: { params: Promise<{ id: string }> }) { return <ManagementContractDetailsPage id={(await params).id} /> }
